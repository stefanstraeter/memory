import './styles/main.scss';
import { initUI } from './ui/navigation';

/* ==========================================================================
	INITIALIZATION
	========================================================================== */

/**
 * Boots the UI once the main bundle is loaded.
 */
initUI();

